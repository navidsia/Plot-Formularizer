# Plot-Formularizer
A desktop application for extracting numerical data from plot images. The app lets you calibrate axes using origin points and known min/max values, digitize points from the image, optionally interpolate between them, and export the results to Excel.
